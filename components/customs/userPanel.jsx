"use client"
import { cn } from '@/lib/utils'
import React from 'react'

export default function UserPanel({className}) {
  return (
    <section className={cn('',className)}>
        
    </section>
  )
}
